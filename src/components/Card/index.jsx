// const Map = () => {
//   const { isLoaded, loadError, userPosition, onLoad } = useMap();

//   return !loadError ? (
//     isLoaded ? (
//       userPosition && (
//         <GoogleMap mapContainerStyle={container} onLoad={onLoad}></GoogleMap>
//       )
//     ) : (
//       <h4>Loader...</h4>
//     )
//   ) : (
//     <h1>O mapa não pôde ser carregado</h1>
//   );
// };
