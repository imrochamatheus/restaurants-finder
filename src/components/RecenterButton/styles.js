import styled from "@emotion/styled";

export const CustomButton = styled.button`
  position: absolute;
  bottom: 2rem;
  left: 0.5rem;
  width: 4rem;
  background: none;
  border: none;
  z-index: 10;
  cursor: pointer;
`;
